### Example .env for HunterCallbackServer

# ----------------------------
# SQLite (quick local testing)
# ----------------------------
DB_DRIVER=sqlite
# For sqlite, DB_DATABASE is a filesystem path (relative or absolute)
DB_DATABASE=./data/hunter_test.sqlite

# API key used by ProxyHunter to query the private API
API_KEY=replace_with_a_strong_api_key_here

# ----------------------------
# MySQL / MariaDB (production-like)
# ----------------------------
# To use MySQL, set DB_DRIVER=mysql and fill the values below.
# Uncomment and edit the values when switching to MySQL.
#DB_DRIVER=mysql
#DB_HOST=127.0.0.1
#DB_PORT=3306
#DB_DATABASE=hunterdb
#DB_USERNAME=hunteruser
#DB_PASSWORD=very_secure_password_here
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=hunter_callback
DB_USERNAME=hunter
DB_PASSWORD=secret
API_KEY=your_secret_api_key
